# Vercel Deployment Rule

- **Default Behavior**: NEVER perform `git commit` or `git push` automatically after editing code.
- **Trigger**: Only perform `git commit` and `git push` to deploy to Vercel when the user explicitly instructs with "push ke vercel" (or an explicit request to push/deploy).
